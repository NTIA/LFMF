name: Validate CITATION.cff

on:
  ## Auto-triggers are disabled by default. Uncomment the push/pull_request
  ## blocks below to enable validation on branches/PRs. Until then,
  ## the workflow runs only via manual dispatch (Actions tab → Run workflow).
  #push:
  #  branches: ["main"]
  #  paths:
  #    - 'CITATION.cff'
  #    - '.github/workflows/cff-validator.yml'
  #pull_request:
  #  branches: ["main"]
  #  paths:
  #    - 'CITATION.cff'
  #    - '.github/workflows/cff-validator.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  Validate-CITATION-cff:
    # Use current(Aug 2026) stable Ubuntu version
    runs-on: ubuntu-24.04
    name: Validate CITATION.cff
    concurrency:
      group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}-citation
      cancel-in-progress: true
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      - name: Validate CITATION.cff
        uses: dieghernan/cff-validator@v5
